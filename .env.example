# Supabase Configuration
# For local development, use Supabase local instance (http://127.0.0.1:54321)
# For production, these should be set in Vercel environment variables

# Public Supabase URL - Safe to expose to browser
PUBLIC_SUPABASE_URL=http://127.0.0.1:54321

# Public Supabase Anon Key - Safe to expose to browser (only has public access)
PUBLIC_SUPABASE_KEY=your-anon-key-here

# Supabase Service Role Key - NEVER expose to browser!
# This key has admin privileges and bypasses Row Level Security
# In production, set this in Vercel environment variables only
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# OpenRouter API Key
OPENROUTER_API_KEY=your-openrouter-api-key

# Mapbox Token - Safe to expose to browser (restricted by domain)
PUBLIC_MAPBOX_TOKEN=your-mapbox-token-here
